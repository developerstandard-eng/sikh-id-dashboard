import ComingSoon from '@/components/ComingSoon';

export default function SavedPage() {
  return (
    <ComingSoon
      title="Saved & Favourites"
      description="Bookmark events, news, and directory listings across the Sikh Group ecosystem and find them all here."
    />
  );
}
